server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/patientdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: medilabo_user
    password: mdp

  jpa:
    hibernate:
      ddl-auto: update          # crée/ajuste la table patients si besoin
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect

  sql:
    init:
      mode: always




